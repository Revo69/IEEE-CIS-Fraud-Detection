# ==============================================================================
# CI/CD Dependencies — только для тестов в GitHub Actions
# Облегчённый набор: без Airflow, без GCP, без тяжёлых библиотек
# ==============================================================================

# Core (нужны для src/ модулей)
duckdb==1.4.4
pandas==3.0.1
pyarrow==23.0.1
numpy>=2.0.0,<3.0.0

# Configuration
pydantic==2.12.5
pydantic-settings==2.5.0
python-dotenv==1.0.1

# Logging
loguru==0.7.3

# Testing
pytest==9.0.2
pytest-cov==7.0.0